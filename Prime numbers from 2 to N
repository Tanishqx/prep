#Print prime numbers from 2 to N
def primeFrom2ToN(n):
    for k in range(2,n+1):
        #check if k is prime ,if it is prime then print k
        is_k_prime=isPrime(k)
        if (is_k_prime):
            print(k)

primeFrom2ToN(100)
